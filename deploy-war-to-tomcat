
node{
    stage('SCM checkout'){
        git 'https://github.com/Latence132/my-app'
    }
    
    stage('Compile package'){
        sh 'mvn clean package'
    }
    /*
    stage('SOnarQube Analysis'){
        withSonarQubeEnv('sonarqube') {
            sh 'mvn sonar:sonar'
        }
    }
    */
    
    sshagent('Deplot to Tomcat') {
    sh 'scp -o StrictHostKeyChecking=no target/*.war ec2-user@15.237.110.35:/opt/tomcat/webapps/'
}
}
